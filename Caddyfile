{
  debug
  admin off
}

# Note: the urls use `http`, not `https` because Render adds `https` at a higher layer
http://meetup-blitz.herokuapp.com,
http://admin.meetup.events {
  reverse_proxy 0.0.0.0:5555
  log
  basicauth {
    {$STUDIO_USERNAME} {$STUDIO_PASSWORD_HASH}
  }
}
