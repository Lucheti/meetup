import { createStyles } from "@mantine/core"

export const LogoStyles = createStyles((theme) => ({
  logo_container: {
    width: "75px",
    cursor: "pointer",
  },
  logo_placeholder: {
    color: "white",
    background: "blue",
  },
}))
